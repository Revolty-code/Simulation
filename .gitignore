# Monolithique tout-inline à la racine — dev local uniquement, non déployé
# (le slash initial force la correspondance à la racine, pas dans dist/)
/Simulation_Revolty.html

# Tooling local (Claude Code, skills, etc.)
.claude/
.agents/
skills-lock.json

# Divers
.DS_Store
Thumbs.db
